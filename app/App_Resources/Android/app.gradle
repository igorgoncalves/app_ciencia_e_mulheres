// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "edu.ifs.ciem"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
