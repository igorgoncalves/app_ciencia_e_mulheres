// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.appmulheres.application"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
